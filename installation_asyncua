#using below command to install asyncua through terminal
pip install asyncua

# if in case throwing error cryptography not installed then try as below
python3.8 -m pip install --upgrade pip setuptools wheel

pip3 install cffi

pip3 install cryptography==3.4.6

pip3 install asyncua 
